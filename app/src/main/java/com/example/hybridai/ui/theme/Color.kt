package com.example.hybridai.ui.theme

import androidx.compose.ui.graphics.Color

// Premium Dark Theme Colors
val TrueBlack = Color(0xFF000000)
val DarkGray = Color(0xFF121212)
val SurfaceGray = Color(0xFF1E1E1E)

val PrimaryAccent = Color(0xFFE2E2E2) // crisp white for primary text
val SecondaryAccent = Color(0xFFA0A0A0) // subdued gray for metadata/status

// Status Indicators
val LocalIndicator = Color(0xFF00E676)    // Bright Green for Local
val OnlineIndicator = Color(0xFF29B6F6)   // Bright Blue for Online
val UserMessageColor = Color(0xFF2B2B2B)  // Slightly lighter than background

// Code block colors (used by MarkdownText)
val CodeBackground = Color(0xFF0D1117)    // GitHub dark code background
val CodeText = Color(0xFF79C0FF)          // GitHub blue-ish monospace text

